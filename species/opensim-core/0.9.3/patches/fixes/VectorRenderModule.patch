diff --git a/OpenSim/Region/CoreModules/Scripting/VectorRender/VectorRenderModule.cs b/OpenSim/Region/CoreModules/Scripting/VectorRender/VectorRenderModule.cs
index 728d28b..1234567 100644
--- a/OpenSim/Region/CoreModules/Scripting/VectorRender/VectorRenderModule.cs
+++ b/OpenSim/Region/CoreModules/Scripting/VectorRender/VectorRenderModule.cs
@@ -148,13 +148,17 @@ namespace OpenSim.Region.CoreModules.Scripting.VectorRender

             // We won't dispose of these explicitly since this module is only removed when the entire simulator
             // is shut down.
+            try
+            {
             lock(thisLock)
             {
                 if(m_graph == null)
                 {
                     Bitmap bitmap = new Bitmap(32, 32, PixelFormat.Format32bppArgb);
                     m_graph = Graphics.FromImage(bitmap);
                 }
             }
+            }
+            catch (Exception) {}
         }

         public void PostInitialise()
@@ -489,6 +493,8 @@ namespace OpenSim.Region.CoreModules.Scripting.VectorRender
         private void GDIDraw(string data, Graphics graph, char dataDelim, out bool reuseable)
         {
             reuseable = true;
+            try
+            {
             Point startPoint = new Point(0, 0);
             Point endPoint = new Point(0, 0);
             Pen drawPen = null;
@@ -803,6 +809,11 @@ namespace OpenSim.Region.CoreModules.Scripting.VectorRender
                 if (myBrush != null)
                     myBrush.Dispose();
             }
+            }
+            catch (Exception)
+            {
+               // GDI+ Not Supported
+            }
         }

         private static void GetParams(char[] partsDelimiter, ref string line, int startLength, ref float x)
