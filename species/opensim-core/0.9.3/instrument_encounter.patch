diff --git a/OpenSim/Framework/EncounterLogger.cs b/OpenSim/Framework/EncounterLogger.cs
new file mode 100644
index 0000000..e69de29
--- /dev/null
+++ b/OpenSim/Framework/EncounterLogger.cs
@@ -0,0 +1,28 @@
+using System;
+using System.IO;
+
+namespace OpenSim.Framework
+{
+    public static class EncounterLogger
+    {
+        // Log path relative to the bin directory where OpenSim.exe runs
+        private static string LogPath = "encounter.log";
+
+        public static void Log(string side, string component, string signal, string payload = "")
+        {
+            string message = $"[ENCOUNTER] [{side}] [{component}] {signal}";
+            if (!string.IsNullOrEmpty(payload))
+            {
+                message += $" | {payload}";
+            }
+
+            // Console.WriteLine(message); // Optional: Print to console
+
+            try
+            {
+                File.AppendAllText(LogPath, $"{DateTime.Now:yyyy-MM-dd HH:mm:ss.fff} {message}{Environment.NewLine}");
+            }
+            catch (Exception) { }
+        }
+    }
+}
diff --git a/OpenSim/Region/CoreModules/ServiceConnectorsOut/Simulation/LocalSimulationConnector.cs b/OpenSim/Region/CoreModules/ServiceConnectorsOut/Simulation/LocalSimulationConnector.cs
index 1234567..89abcdef 100644
--- a/OpenSim/Region/CoreModules/ServiceConnectorsOut/Simulation/LocalSimulationConnector.cs
+++ b/OpenSim/Region/CoreModules/ServiceConnectorsOut/Simulation/LocalSimulationConnector.cs
@@ -1,5 +1,6 @@
 using System;
 using OpenSim.Framework;
+using OpenSim.Framework; // Ensure namespace is available

 namespace OpenSim.Region.CoreModules.ServiceConnectorsOut.Simulation
 {
@@ -50,6 +51,7 @@
         public bool CreateAgent(GridRegion destination, AgentCircuitData aCircuit, uint teleportFlags, AgentData agent, out string reason)
         {
             // Field Mark: Login - Circuit Provision (Local)
+            EncounterLogger.Log("SERVER", "LOGIN", "CIRCUIT PROVISION", $"Circuit: {aCircuit.circuitcode}, Region: {destination.RegionName}");
             return m_scene.NewUserConnection(aCircuit, teleportFlags, agent, out reason);
         }
     }
diff --git a/OpenSim/Region/Framework/Scenes/Scene.cs b/OpenSim/Region/Framework/Scenes/Scene.cs
index abcdef1..2345678 100644
--- a/OpenSim/Region/Framework/Scenes/Scene.cs
+++ b/OpenSim/Region/Framework/Scenes/Scene.cs
@@ -100,6 +100,7 @@
         public bool NewUserConnection(AgentCircuitData aCircuit, uint teleportFlags, AgentData agent, out string reason)
         {
             // Existing logic...
             bool success = base.NewUserConnection(aCircuit, teleportFlags, agent, out reason);
+            if (!success) EncounterLogger.Log("SERVER", "LOGIN", "CIRCUIT FAIL", $"Reason: {reason}");
             return success;
         }

@@ -200,6 +201,7 @@
         public void UseCircuitCode(GridClient client, UseCircuitCodePacket uccp)
         {
             // Field Mark: UDP - UseCircuitCode
+            EncounterLogger.Log("SERVER", "UDP", "RECV UseCircuitCode", $"CircuitCode: {uccp.CircuitCode.Code}, Session: {uccp.CircuitCode.SessionID}");

             // ... verification logic ...

             if (authenticated)
             {
                 // ...
             }
             else
             {
+                EncounterLogger.Log("SERVER", "UDP", "REJECT UseCircuitCode", $"Unauthorized Circuit: {uccp.CircuitCode.Code}");
             }
         }
     }
diff --git a/OpenSim/Region/ClientStack/Linden/UDP/LLClientView.cs b/OpenSim/Region/ClientStack/Linden/UDP/LLClientView.cs
index 9876543..7654321 100644
--- a/OpenSim/Region/ClientStack/Linden/UDP/LLClientView.cs
+++ b/OpenSim/Region/ClientStack/Linden/UDP/LLClientView.cs
@@ -50,6 +50,7 @@
         public void SendRegionHandshake(RegionInfo regionInfo, RegionHandshakeArgs args)
         {
             // Field Mark: UDP - RegionHandshake
+            EncounterLogger.Log("SERVER", "UDP", "SEND RegionHandshake", $"Region: {m_scene.RegionInfo.RegionName}");
             base.SendRegionHandshake(regionInfo, args);
         }

@@ -60,6 +61,7 @@
         public void SendAgentMovementComplete(UUID agentID, Vector3 pos, Vector3 look)
         {
+            EncounterLogger.Log("SERVER", "UDP", "SEND AgentMovementComplete", $"Pos: {pos}, Look: {look}");
             base.SendAgentMovementComplete(agentID, pos, look);
         }

@@ -70,6 +72,7 @@
         public void SendLayerData(float[] map)
         {
+            EncounterLogger.Log("SERVER", "UDP", "SEND LayerData", "Terrain Patches");
             base.SendLayerData(map);
         }

@@ -85,6 +88,7 @@
         public void CheckHeartbeat()
         {
             // ... existing timeout logic ...
             if (timedOut)
             {
+                EncounterLogger.Log("SERVER", "UDP", "TIMEOUT", $"Agent: {client.Name}, LastActive: {timeoutTicks}ms ago");
                 Close();
             }
         }
     }
diff --git a/OpenSim/Services/LLLoginService/LLLoginService.cs b/OpenSim/Services/LLLoginService/LLLoginService.cs
index 567890a..b123456 100644
--- a/OpenSim/Services/LLLoginService/LLLoginService.cs
+++ b/OpenSim/Services/LLLoginService/LLLoginService.cs
@@ -30,6 +30,7 @@
         public LoginResponse Login(LoginRequest request)
         {
             // Field Mark: Login - XMLRPC Entry
+            EncounterLogger.Log("SERVER", "LOGIN", "RECV XML-RPC login_to_simulator", $"User: {firstName} {lastName}, Viewer: {clientVersion}, Channel: {channel}, IP: {clientIP}");

             // ... Authentication ...

             if (!authenticated)
             {
+                EncounterLogger.Log("SERVER", "LOGIN", "AUTH FAIL", $"User: {firstName} {lastName}");
                 return response;
             }

+            EncounterLogger.Log("SERVER", "LOGIN", "AUTH SUCCESS", $"User: {firstName} {lastName}");

             // ... Placement ...

             // Field Mark: Login - XMLRPC Response
+            EncounterLogger.Log("SERVER", "LOGIN", "SEND XML-RPC Response", "Success");
             return response;
         }
     }
