#!/bin/bash
REPO_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"

. $REPO_ROOT/instruments/substrate/observatory_env.bash
. $REPO_ROOT/species/opensim-core/plugins/oscsc.bash

cspp "$@" | grep -E --color=always "error |$"
