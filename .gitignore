# ----------------------------------------------------------------------
# DENY BY DEFAULT
# ----------------------------------------------------------------------
# Block everything in the root to prevent accidental artifact commits.
# Using '/*' instead of '*' prevents recursive blocking of whitelisted directories' contents.
/*
# specific intent (technically but not notionally redundant with /*)
/vivarium

# ----------------------------------------------------------------------
# SAFE HARBORS (Explicitly Allowed)
# ----------------------------------------------------------------------
# Allow the repository configuration itself
!/.gitignore
!/Makefile
!/README.md
!/AGENTS.md
!/EXPEDITION_LOG.md

# Allow primary source directories
!/species/
!/instruments/
!/observatory/
!/journals/
